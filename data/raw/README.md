# Raw Data

Raw datasets are intentionally not committed to the repository.

Use the project commands below to generate or download local data:

```bash
python -m src.data.download
python -m src.data.preprocess
```

The Docker training flow also prepares the required data automatically.
